# Semi-Weekly Capstone Sync — Meeting Minutes  
**Date:** 18 Feb 2026  
**Time:** 11:58pm (recording timestamp)  
**Duration:** 42m 17s  
**Meeting type:** Team sync / sprint check-in + Friday session planning

## Attendees
- William Clubine
- Gregory Archer
- Mayowa Adesanya
- Speaker 1 (Khach)
- **Absent:** Jasiri (not present), Nick (not present)

---

## Agenda
1. Quick sprint review / board walkthrough  
2. Updates by team member (Greg, Speaker 1, Mayowa)  
3. Unit testing plan for the sprint  
4. Friday older-adult session logistics + prep  
5. Misc. issues (mic permission UX, CI formatting)

---

## Notes / Discussion

### 1) Meeting start + audio issue
- Will’s microphone audio was initially distorted; issue resolved after switching/adjusting mic (EarPods mic suspected).

### 2) Sprint recap / board review
- Will noted most items from the previous sprint were already discussed previously.
- **New UI tweak:** Added a **session number** to the Stop button label (e.g., “Stop Session 001”) so it can be written down during sessions.

### 3) Unit testing focus (second half of sprint)
- Team agreed unit tests should be prioritized **later in the sprint** (after closing other items).
- Current state:
  - There is **one existing unit test suite** (generated as part of transcript chunk ordering work; Will used Copilot to prototype it).
  - Mario has a PR that adds two more tests (mentioned by Will).
- Approach discussed:
  - Add tests per **service**, likely naming test classes by service.
  - Focus on backend/services (not front-end UI tests).
  - Idea: assign each person a set of classes/services and write tests method-by-method for reasonable coverage.

### 4) Gregory — “Intelligent prompt dismissal”
- Greg is implementing **intelligent prompt dismissal** to reduce user interaction (prompts auto-dismiss after a period).
- Key concern: avoid incorrect behavior when multiple prompts occur (ensure dismissing one prompt doesn’t override another unexpectedly).
- Plan:
  - Greg aims to push a PR **today** (after testing).
  - Team will review quickly before Friday if possible.
- Decision framing:
  - If risky/unstable, team may skip it for Friday; otherwise, it improves usability.

### 5) Instruction manual updates (for Dr. Farrokhi)
- Will recalled a prior request to keep the instruction manual updated when things change.
- Plan:
  - During Friday testing, update the document live if workflows differ from the doc.
  - Potential addition: expand the manual to explain newer app functions (export/config), beyond just start/stop.

### 6) Friday session logistics (older adults)
- **Arrival time:** Aim for **9:30am** (Dr. Farrokhi expected around 9:30; first participant at 10:30).
- **Session window:** Scheduled **10:30am–2:30pm** (staying the entire time is preferred but not mandatory).
- Expected roles:
  - Dr. Farrokhi leads the session with older adults.
  - Team primarily supports setup, observes, and fixes issues as needed.
  - Prior session took longer than expected due to setup hurdles; this time should be smoother since the Vision Pro likely wasn’t wiped.

### 7) Will — CI formatting/linting + “verify”
- Formatting/linting CI was temporarily disabled to avoid noisy warnings while multiple PRs are open.
- Plan: once PR traffic settles, run formatter across the codebase, then share config for auto-format on commit.
- Will may attempt a “verify” step tomorrow (needs to send an ID) if it could help for Friday.

### 8) Data specification dependency
- Will is still waiting for Dr. Farrokhi to provide a more formal **data specification** (format/content).
- Once received:
  - Will may coordinate collecting data into CSV/JSON.
  - This would also help Jasiri’s web work (upload to DB + display).

### 9) Speaker 1/Khach — Integrate multiple LLM providers + mic permission UX
**LLM provider integration**
- Speaker 1 asked how “integrate multiple LLMs” should work.
- Will explained current behavior:
  - Config menu has checkboxes to enable models (currently OpenAI category), and the session picks an enabled model at random.
- Proposed next step:
  - Add additional provider categories (e.g., **Google Gemini** and **Apple on-device Foundation Models / AFM**).
  - Implementation guidance:
    - Add another `LLMProvider` implementation in `LLMService` (same protocol; implement `generate`).
    - Add provider-specific model enums + update config UI/model similarly to OpenAI’s pattern.
- Timeline:
  - Not needed for Friday.
  - Target: ideally **within this sprint** (~2 weeks), if feasible.

**Microphone permission error messaging**
- Speaker 1 reported the old custom “mic permission not granted” UI no longer appears.
- Current behavior: user sees a non-user-friendly error (“operation couldn’t be completed… SpeechServiceError… error 3”).
- Proposed quick fix:
  - Add a specific catch case for microphone permission error (e.g., `SpeechServiceError.permissionNotGranted`) and show a clear, user-friendly message via `launchError`.

### 10) Mayowa — LLM trigger evaluator + simulator warning issue
- Mayowa completed the **LLM trigger evaluator** change and adjusted it to behave like other evaluators (removed special fallback behavior).
- Will requested **do not merge until after Friday** (PR remains open/on branch).
- Second ticket:
  - Mayowa misunderstood “simulator” initially; Will clarified he meant VisionOS simulator on laptop.
  - Mayowa will investigate a simulator warning/error and validate any fix still launches and runs.


---

## Decisions
- **Unit tests** will be a second-half sprint focus; aim for reasonable coverage across service methods.
- **Intelligent prompt dismissal** will be included only if it’s stable enough after review; otherwise skip for Friday.
- **Friday arrival time:** team targets **9:30am** for setup buffer.

---

## Action Items
- **Greg:** Finish testing + push PR for intelligent prompt dismissal; ensure multiple prompts don’t conflict. *(Target: today / ASAP before Friday)*
- **All (review support):** If PR is up in time, review prompt dismissal changes quickly (priority: avoid breaking Friday build).
- **Speaker 1/Khach:** Implement/patch clearer microphone-permission error messaging (specific catch + friendly `launchError`). *(Before Friday if possible)*
- **Mayowa:** Continue simulator warning investigation + test fix in simulator. *(After starting now; merge after Friday)*
- **Will:** Revisit CI formatting/linting once PR load is lower; run formatter across codebase and share auto-format config. *(When PR traffic calms)*
- **Will:** Follow up with Dr. Farrokhi for formal data spec; prepare for CSV/JSON collection once clarified. *(Ongoing)*
- **Team (Friday):** Update instruction manual live during session if workflow differs; consider adding explanation of export/config features.

---

## Next Key Date
- **Friday session:** arrive **9:30am**, session scheduled **10:30am–2:30pm**.
